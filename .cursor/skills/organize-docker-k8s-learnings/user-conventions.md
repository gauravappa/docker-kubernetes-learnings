# User conventions

**Authority:** Rules in this file override defaults in [folder-structure.md](folder-structure.md) and [note-template.md](note-template.md). Both **organize-docker-k8s-learnings** and **polish-docker-k8s-note** must read this file first on every invocation.

**Maintenance:** When the user specifies folder layout, naming, note structure, tone, or formatting preferences (in the same message as organize/polish, or in a dedicated conventions message while either skill is active), append or edit the relevant section below and add a changelog entry. Do not drop existing rules unless the user explicitly revokes them.

---

## Folder structure

<!-- User-defined layout rules. Examples:
- Use `notes/` instead of topic README.md at repo root for drafts
- All Compose files live under `docker/compose/<topic>/examples/`
- Lab folders must use format `labs/YYYY/MM/<slug>/`
-->

*(No user overrides yet — defaults in folder-structure.md apply.)*

---

## Writing format

<!-- User-defined note style. Examples:
- Always include a "Commands cheat sheet" section at the end
- Use first person ("I ran") not neutral voice
- Max 5 takeaways; label severity with emoji
- Date format: DD Mon YYYY
-->

*(No user overrides yet — defaults in note-template.md apply.)*

---

## Changelog

| Date | Section | Change |
|------|---------|--------|
| — | — | *(entries added when conventions are updated)* |
